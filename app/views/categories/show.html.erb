<p id="notice"><%= notice %></p>

<p>
  <b>Name:</b>
  <%= @category.name %>
</p>

<table>
  <tr>
    <th>Posts</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>
<% @post.each do |post| %>
  <tr>
    <td><%= link_to post.title, post %></td>
  </tr>
  <tr>
    <td><%= link_to image_tag(post.photo.url(:small)), post %></td>
  </tr>
<% end %>
</table>

<%= link_to 'Edit', edit_category_path(@category) %> |
<%= link_to 'Back', root_path %>
